import XCTest
@testable import AdaptiveNavigator

final class AdaptiveNavigatorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AdaptiveNavigator().text, "Hello, World!")
    }
}
